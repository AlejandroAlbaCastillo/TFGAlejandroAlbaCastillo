/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java application project to get you started.
 * For more details take a look at the 'Building Java & JVM projects' chapter in the Gradle
 * User Manual available at https://docs.gradle.org/7.5/userguide/building_java_projects.html
 */

plugins {
    // Apply the application plugin to add support for building a CLI application in Java.
    id 'application'
    id 'org.openjfx.javafxplugin' version '0.0.13'
}

repositories {
    // Use Maven Central for resolving dependencies.
    mavenCentral()
}

javafx {
    version = "21"
    modules = ['javafx.controls', 'javafx.graphics','javafx.fxml', 'javafx.swing', 'javafx.web'] //Necesita módulo swing para tratar con imágenes
}



dependencies {
    // Use JUnit Jupiter for testing.
    testImplementation 'org.junit.jupiter:junit-jupiter:5.8.2'

    // This dependency is used by the application.
    implementation 'com.google.guava:guava:31.0.1-jre'

	//JasperReports
    implementation 'com.lowagie:itext:2.1.7'
    implementation 'net.sf.jasperreports:jasperreports:6.18.0'

    // Otros componentes necesarios para JasperReports
	implementation 'org.apache.groovy:groovy:4.0.15'


    //Añadidas
    //Si queremos que funcione con MYSQL, 1) descomentamos la implementation, 2) creamos el contenedor
    //LAMP exponiendo el 3306 y 3) Cambiamos la cadena de conexión en ControladorLibros
    implementation 'mysql:mysql-connector-java:8.0.32'
    //sudo docker run --name LAMP --restart=always  -p "80:80" -p "3306:3306" -v /home/usuario/app:/app mattrayner/lamp:latest-1804
    implementation 'org.openjfx:javafx:19.0.2'
    //implementation 'org.mariadb.jdbc:mariadb-java-client:3.1.2'
}

application {
    // Define the main class for the application.
    mainClass = 'Proyecto.Main'
}

tasks.named('test') {
    // Use JUnit Platform for unit tests.
    useJUnitPlatform()
}

sourceSets {
  main {
    resources {
        srcDirs = ["src/main/java"]
        includes = ["**/*.fxml", "**/*.css", "**/*.jasper","**/*.mp3","**/*.html","**/*.png"]
    }
  }
}
jar{
    duplicatesStrategy 'exclude'
    manifest{
        attributes 'Main-Class': 'Proyecto.Main'
    }
}

